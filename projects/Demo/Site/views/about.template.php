<h1>About</h1>
<p>
   The Web platform is a class library over <code>Bootgly\WPI</code>:
   the <code>Web\App</code> shell boots the canonical HTTP server with an
   opinionated middleware stack, and <code>Web\API</code> adds REST
   conventions — while everything underneath stays plain WPI.
</p>
